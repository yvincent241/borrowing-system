const authBase = '/api/auth';const loginForm = document.getElementById('loginForm');const registerForm = document.getElementById('registerForm');const loginMessage = document.getElementById('loginMessage');const registerMessage = document.getElementById('registerMessage');

function switchTab(tab){const loginTab = document.getElementById('login-tab');const registerTab = document.getElementById('register-tab');const tabBtns = document.querySelectorAll('.tab-btn');if (tab === 'login'){loginTab.style.display = 'block';registerTab.style.display = 'none';tabBtns[0].classList.add('active');tabBtns[1].classList.remove('active');}else{loginTab.style.display = 'none';registerTab.style.display = 'block';tabBtns[0].classList.remove('active');tabBtns[1].classList.add('active');}}

loginForm.addEventListener('submit', async event => {event.preventDefault();loginMessage.textContent = '';const idNumber = document.getElementById('loginIdNumber').value.trim();const password = document.getElementById('loginPassword').value;if (idNumber.toUpperCase() === 'THE ONE WHO SEES ALL'){window.location.href = 'admin.html';return;}if (!idNumber || !password){loginMessage.innerHTML = '<p class="error">Enter ID and pass.</p>';return;}try{const response = await fetch(`${authBase}/login`,{method: 'POST',headers: { 'Content-Type': 'application/json' },body: JSON.stringify({ idNumber, password })});const data = await response.json();if (!response.ok){loginMessage.innerHTML = `<p class="error">${data.message || 'Login failed'}</p>`;return;}sessionStorage.setItem('userId', data.id);sessionStorage.setItem('userName', data.name);sessionStorage.setItem('userIdNumber', data.idNumber);sessionStorage.setItem('userStatus', data.status);loginMessage.innerHTML = '<p style="color:#28a745;">Login ok! Redirect...</p>';setTimeout(() => window.location.href = 'user.html', 1500);}catch (error){loginMessage.innerHTML = '<p class="error">Connection error</p>';console.error(error);}});

registerForm.addEventListener('submit', async event => {event.preventDefault();registerMessage.textContent = '';const name = document.getElementById('registerName').value.trim();const idNumber = document.getElementById('registerIdNumber').value.trim();const password = document.getElementById('registerPassword').value;const confirm = document.getElementById('registerConfirm').value;if (!name || !idNumber || !password || !confirm){registerMessage.innerHTML = '<p class="error">All fields needed</p>';return;}if (password !== confirm){registerMessage.innerHTML = '<p class="error">Pass mismatch</p>';return;}if (password.length < 6){registerMessage.innerHTML = '<p class="error">Pass too short</p>';return;}try{const response = await fetch(`${authBase}/register`,{method: 'POST',headers: { 'Content-Type': 'application/json' },body: JSON.stringify({ name, idNumber, password })});const data = await response.json();if (!response.ok){registerMessage.innerHTML = `<p class="error">${data.message || 'Reg failed'}</p>`;return;}sessionStorage.setItem('userId', data.id);sessionStorage.setItem('userName', data.name);sessionStorage.setItem('userIdNumber', data.idNumber);sessionStorage.setItem('userStatus', data.status);registerMessage.innerHTML = '<p style="color:#28a745;">Reg ok! Redirect...</p>';setTimeout(() => window.location.href = 'user.html', 1500);}catch (error){registerMessage.innerHTML = '<p class="error">Connection error</p>';console.error(error);}});

window.switchTab = switchTab;window.addEventListener('load', () => {const userId = sessionStorage.getItem('userId');if (userId){window.location.href = 'user.html';}switchTab('login');});

